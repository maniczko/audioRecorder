```json
{
  "name": "your-project-name",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.12.1",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "audit": "pnpm audit --json > audit-report.json"
  },
  "devDependencies": {
    "jest": "^27.0.0",
    "eslint": "^7.0.0"
  }
}
```