import { describe, test } from 'vitest';

// These tests require backend mocking which is complex to set up properly
describe('workspaceService', () => {
  test.skip('updates member role locally with normalized role', async () => {});
  test.skip('calls remote endpoint with normalized member role', async () => {});
});
